# The FastAPI chat service in ../api.
# Next.js reads .env* from this directory only — it does NOT read the repo-root
# .env that scripts/ and api/ use, so this variable lives here rather than there.
# NEXT_PUBLIC_ vars are inlined into the browser bundle at build time.
NEXT_PUBLIC_API_URL=http://localhost:8000
